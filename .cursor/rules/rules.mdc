---
alwaysApply: true
---
### Мои базовые цели и задачи
- мне нравится трейлраннинг и я хочу пробежать в следующем сезоне (весна-лето-осень 2026 года) несколько трейлов в горах длиной 50-60 км с набором до 3500 метров
- также я хочу улучшить свою общую физическую форму за счет кроссфит тренировок

### Упражнения, которые я не люблю
- фермерская прогулка

### Назначение
Эти правила помогают ассистенту работать с репозиторием тренировок: поддерживать единые форматы, правильно вносить записи в CSV/MD и не ломать структуру файлов.

### Общие правила
- **Язык**: русский.
- **Пути**: использовать абсолютные пути в операциях.
- **Кодировки/разделители**: UTF-8; перенос строк `\n`; разделитель CSV — запятая.
- **Числа**: в `2025 training log.csv` допускаются и запятая, и точка как десятичный разделитель. Предпочтительно сохранять стиль, уже используемый в конкретном файле/строке (если рядом значения с запятой — писать с запятой и оборачивать в кавычки; с точкой — без кавычек).
- **Даты**: формат `ДД.ММ.ГГГГ` (например, `14.10.2025`). День недели — коротко: `пн, вт, ср, чт, пт, сб, вс`.
- **Единицы**: время — минуты (целое или диапазон), дистанция — км, набор — метры, пульс — уд/мин, темп — `м:с/км`.
- **Без сети/скриптов**: не запрашивать интернет; не добавлять сторонние зависимости.

### Структура репозитория (ключевое)
- `2025 training log.csv` — журнал фактических тренировок (по дням месяца).
- `2025 training plan.csv` — недельный/дневной план с эмодзи и зонами.
- `pulse zones.csv` — зоны ЧСС (Карвонен), Z1–Z5.
- `2025 detailed workouts plans/` — подробные планы отдельных сессий в Markdown.
- `2025 raw workouts data/` — сырые выгрузки с устройств (оставлять неизменными, не переписывать).
- `workouts/` — архив экспортов (если присутствует). Не редактировать содержимое, использовать только для сверки.

### Правила для `2025 training log.csv`
- **Столбцы (порядок фиксирован)**:
  1. Дата
  2. День недели
  3. Тип тренировки (например, `Бег`, `Зал`, `Кроссфит`)
  4. Подтип / Цель (например, `Восстановительный`, `Лонгран`)
  5. Время (мин)
  6. Дистанция (км)
  7. Средний пульс
  8. Макс. пульс
  9. Средний темп (мин/км)
  10. Набор (м)
  11. Самочувствие (1–5)
  12. Вес (кг)
  13. Комментарий / Ощущения
  14. Усталость (1–5)
- **Изменять только пустые ячейки соответствующего дня**. Новые фактические записи — дополнять строку с этой датой, не переставляя столбцы и не удаляя заголовок.
- **Темп**: если отсутствует, можно вычислить как `время_в_минутах / дистанция` и округлить до ближайшей секунды; форматировать `м:с` (например, `5:41`). Если в файле уже указан темп или данные неконсистентны — не пересчитывать.
- **Кавычки**: если используется запятая как десятичный разделитель (например, `"8,02"`), значение оборачивать в кавычки.
- **Пример строки** (как в журнале):

```csv
07.10.2025,вт,Бег,Восстановительный,26,"4,42",146,162,5:55,60,,,,
```

### Правила для `2025 training plan.csv`
- Сохранять структуру таблицы и повторяющиеся блоки заголовков (переходы недель разделены пустыми строками).
- Не удалять эмодзи и диапазоны (`45–60`), использовать длинное тире `–` как в файле.
- Поля: `День`, `Тип тренировки`, `Подтип / Цель`, `Время (мин)`, `Примечание / Содержание` (или `Структура / Задачи`), `Зона ЧСС / Темп`, `Цель дня`.
- Пример строки:

```csv
вт 14.10,🏃 Бег,Восстановительный,46,8.2 км @ 5:41/км,Z1–Z2 (133–158),возвращение к форме
```

### Правила для `pulse zones.csv`
- Оставлять метод Карвонена и обозначения Z1–Z5; диапазоны и цели — как в исходнике.
- Пример строки:

```csv
Z2,65–75%,145–158,аэробная база
```

### Подробные планы в `2025 detailed workouts plans/`
- Имена файлов: `ГГГГ-ММ-ДД описание.md` (например, `2025-10-17 gym.md`).
- Рекомендованная структура: цель, интенсивность (RPE/зоны), оборудование, разминка, основной блок с интервалами/подблоками, финал/EMOM (если есть), заминка, адаптации при дискомфорте.
- Форматировать списками; использовать единицы и зоны консистентно с планом и журналом.

### Сырые данные устройств
- Сохранять выгрузки в `2025 raw workouts data/` без изменений. Если нужно — читать, агрегировать и переносить итоговые значения в журнал вручную.
- Переименования/исправления в `workouts/` не вносить; использовать только для сверки.

### Типовые действия ассистента
- **Добавить запись о пробежке в журнал**: найти строку по дате в `2025 training log.csv` добавить все возможные данные из соответствующего файла в `2025 raw workouts data/`.
- **Составить по запросу детальный план на тренировку**: создать новый MD-файл в `2025 detailed workouts plans/` по шаблону выше, без изменения существующих.
